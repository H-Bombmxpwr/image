# --- Web ---
Flask==3.0.3
Werkzeug==3.0.3
itsdangerous==2.2.0
Jinja2==3.1.4
MarkupSafe==2.1.5
gunicorn==25.3.0

# --- Imaging core ---
Pillow==10.4.0
pillow-heif>=0.16.0
numpy==2.0.2

# --- Heavy server-side features enabled for deployed builds ---
seam-carving>=0.1.2
rembg>=2.0
onnxruntime>=1.18,<2

# Optional fallback if seam-carving wheels are unavailable on a target platform.
# scikit-image>=0.24.0
